import AboutSection from "@/components/SectionComponents/AboutSection"
export default function AboutPage() {
    return (
        <section id="aboutPage" className="relative min-h-dvh bg-linear-to-t from-(--background) via-(--background) to-(--primary)">
            <AboutSection />
        </section>
    )
}