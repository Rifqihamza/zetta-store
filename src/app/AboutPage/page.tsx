import AboutSection from "@/components/SectionComponents/AboutSection"

export default function AboutPage() {
    return (
        <section id="aboutPage" className="relative w-full max-w-7xl mx-auto min-h-screen">
            <AboutSection />
        </section>
    )
}